{
  "id": "m9-reserialize-asset-extension",
  "title": "Reserialize round-trips a .asset asset",
  "milestone": "m9",
  "engineId": "unity",
  "order": 102,
  "requirementLevel": "optional",
  "tags": ["m9", "reserialize", "extension-matrix", "plan-1"],
  "automatedCoverage": [
    "ReserializeAssetsToolTests.SupportedExtensions_IncludesAllRequiredFormats"
  ],
  "steps": [
    {
      "id": "info",
      "type": "info",
      "title": "Prerequisites",
      "body": "Bridge running. Optional extension-matrix check (automated-covered): confirm `.asset` is accepted and round-tripped. The supported-extension surface is asserted by the automated test; this is a runnable confidence pass against a live ScriptableObject/profile asset."
    },
    {
      "id": "setup-fixture",
      "type": "setup",
      "title": "Stage a non-canonical copy of an .asset",
      "actions": [
        {
          "action": "fs_copy",
          "from": "Assets/Tests/GateDeltaFixtures/DeltaTest.asset",
          "to": "{fixtureRoot}/ExtAsset.asset"
        },
        {
          "action": "fs_patch",
          "path": "{fixtureRoot}/ExtAsset.asset",
          "patches": [
            {
              "op": "trim_trailing_whitespace"
            }
          ]
        }
      ]
    },
    {
      "id": "prompt",
      "type": "agent_prompt",
      "title": "Run reserialize on the .asset",
      "tool": "unity_open_mcp_reserialize",
      "payload": {
        "paths": ["{fixtureRoot}/ExtAsset.asset"],
        "gate": "enforce"
      }
    },
    {
      "id": "expected",
      "type": "expected",
      "title": "Expected outcomes",
      "items": [
        "mutation.success is true (extension accepted)",
        "mutation.output.reserialized contains the .asset path; totalCount: 1",
        "gate.validation.passed is true",
        "file on disk is canonical Unity YAML"
      ]
    },
    {
      "id": "actual",
      "type": "actual",
      "title": "Paste the agent output (raw envelope)"
    },
    {
      "id": "done",
      "type": "mark_done",
      "title": "Mark this test complete"
    }
  ],
  "reset": {
    "afterStep": {
      "setup-fixture": {
        "actions": [{ "action": "fs_delete", "paths": ["{fixtureRoot}"] }]
      }
    },
    "note": "Optional, automated-covered scenario. The supported-extension contract is asserted by ReserializeAssetsToolTests; this runnable pass is for operator confidence against a live .asset."
  }
}
