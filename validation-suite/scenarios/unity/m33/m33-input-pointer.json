{
  "id": "m33-input-pointer",
  "title": "uGUI: event delivery and interaction truth",
  "milestone": "m33",
  "engineId": "unity",
  "requirementLevel": "required-core",
  "tags": [
    "input-simulation",
    "live-editor"
  ],
  "steps": [
    {
      "id": "setup",
      "type": "info",
      "title": "Prerequisites",
      "body": "Open the demo in a Unity Editor with the local bridge, uGUI and Input System installed. Build mcp-server first. This replay temporarily enters play mode, preserves existing unsaved scene content, and never saves scenes. Close unrelated test runs before starting."
    },
    {
      "id": "replay",
      "type": "agent_prompt",
      "title": "Run the live regression replay",
      "payload": "From the repository root run: node scripts/mcp-input-simulation.mjs --fixture pointer --project <absolute-demo-path> --json-out <local-report-path>. Inspect the assertions and attach the JSON report. The script owns fixture cleanup and restores the initial play/edit state."
    },
    {
      "id": "expected",
      "type": "expected",
      "title": "Expected outcomes",
      "items": [
        "Every assertion passes, including drag release order, drop delivery, no-handler drop=false, held hover, endpoint errors, ambiguity, and occlusion reporting.",
        "All temporary scene objects are destroyed and the original EventSystem and play/edit state are restored."
      ]
    },
    {
      "id": "actual",
      "type": "actual",
      "title": "Record Editor version, assertion count and report path"
    },
    {
      "id": "done",
      "type": "mark_done",
      "title": "Record the result"
    }
  ],
  "reset": {
    "note": "The replay restores its temporary player loop/input settings/devices and destroys fixture objects in finally blocks. It exits play mode only if it entered it. No disk fixture or scene save is required."
  },
  "order": 20
}
